# 2026-09-15 연산자 및 제어문 학습 내용

- 산술·비교 연산자와 동등성(`==`), 동일성(`is`) 비교
- 논리 연산자(`and`, `or`) 사용
- 리스트 인덱싱과 슬라이싱(`start:stop:step`) 실습
- `while` 반복문과 `break`, `else` 사용
- `for` 반복문과 `range()` 함수 활용
- `enumerate()`로 인덱스와 값 함께 순회
- `zip()`으로 여러 시퀀스의 요소 결합
- f-string, `format()`, `%`를 이용한 문자열 포맷팅